# agent-execute: Execute stage image for Java migrations.
#
# Build context must be the repository root:
#   podman build -t agent-execute -f images/agent-execute/Containerfile .

FROM agent-java-base

COPY skills/execute/ /opt/skills/execute/

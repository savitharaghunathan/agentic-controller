apiVersion: skillimage.io/v1alpha1
kind: SkillCard
metadata:
  name: verify-java
  namespace: konveyor
  version: "1.0.0"
  description: >
    Runs mvn clean compile, parses errors, applies conservative fixes, and
    iterates until the build passes or max iterations are reached.
  tags:
    - java
    - migration
    - verification
    - maven
spec:
  prompt: SKILL.md
